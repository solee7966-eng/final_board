# 운영용 yml 파일(AWS 운영용 DB 설정)

spring:
  config:
    activate:
      on-profile: prod

  datasource-myspringbootuser:
    driver-class-name: oracle.jdbc.OracleDriver
    jdbcUrl: jdbc:oracle:thin:@//172.31.38.176:1521/XEPDB1
    username: final_orauser_2
    password: sistsix
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000


file:
  upload-dir: /app/data/file_upload
  photoupload-dir: /app/data/file_photoupload
  emailattachfile-dir: /app/data/file_emailattachfile
  images-dir: /app/data/file_images


eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/